subprojects {
    version = '1.0.0-SNAPSHOT'

    apply plugin: 'java'
    apply plugin: 'eclipse'

    repositories {
        mavenCentral()
    }

    sourceCompatibility = '1.8'
    targetCompatibility = '1.8'

    task getVersion {
        doLast {
            println version
        }
    }
}
